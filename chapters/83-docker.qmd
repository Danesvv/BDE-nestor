# Docker: gestión de contenedores e imágenes {#sec-anexo-docker}

```{r}
#| label: setup-j-eval-j-plot
#| code-fold: true
#| include: false
source("../j_eval_j_plot.r")
```

```{r}
#| label: setup-variables-sistema-r
#| include: false

# 1. Registro de variables en el proceso padre para persistencia y herencia en Knitr
Sys.setenv(
  # Directorio relativo de almacenamiento de respaldos (resuelto desde chapters/)
  PLOTS_DIR = "../plots"
)

```


```{bash}
#| label: setup-variables-entorno
#| eval: true
#| echo: true
# ===========================================================================
# 1. Directorio de ploteos
# ===========================================================================
# Resolución condicional: valida si la sesión se ubica en la raíz o en chapters/
if [ -d "plots" ]; then
    export PLOTS_DIR="plots"
else
    export BACKUP_DIR="../plots"
fi

# ===========================================================================
# 2. Verificación de variables asignadas
# ===========================================================================
echo "Directorio de ploteos: ${PLOTS_DIR}"

```

El sistema Docker opera como un motor de virtualización [@docker2025] sobre el cual se desplegarán las máquinas virtuales basadas en Linux. Estas infraestructuras permiten la ejecución integrada de código analítico en Python, R y Julia, junto al soporte persistente de bases de datos geográficas.


## Instalación del software

Descargue la versión oficial del orquestador desde el repositorio principal:

- **URL oficial**: <https://www.docker.com/products/docker-desktop/>
- Descargue el paquete instalador correspondiente a la arquitectura de hardware del equipo local.

::: {.panel-tabset}

### Sistema anfitrión (Windows)

Para garantizar un rendimiento óptimo en Windows, el motor debe ejecutarse sobre la arquitectura nativa de virtualización de WSL 2 en lugar del hipervisor emulado Hyper-V.

1. Abra una terminal de PowerShell con privilegios de administrador y actualice los componentes del subsistema:

```powershell
#| label: setup-wsl-update
#| eval: false
wsl --update

```

2. Ejecute el instalador descargado. Durante el asistente de configuración:
* Asegúrese de marcar la casilla **"Use WSL 2 instead of Hyper-V"**.
* Si el asistente solicita reiniciar el sistema al finalizar la descompresión de binarios, reinicie el equipo para inicializar el controlador de virtualización.


3. **Instalación en una unidad secundaria (opcional)**: Si la unidad principal del sistema no dispone de espacio suficiente para la instalación de los binarios base, es posible forzar una ruta de instalación personalizada desde PowerShell como administrador:

```powershell
#| label: setup-install-docker-dir
#| eval: false
Start-Process "Docker Desktop Installer.exe" -ArgumentList "install", "--installation-dir=<unidad>:\Docker" -Wait

```

### Procedimiento general (agnóstico)

En sistemas operativos tipo POSIX o arquitecturas alternativas:

1. Instale el paquete siguiendo los estándares del sistema anfitrión (gestores de paquetes `apt`, `brew` o asistentes nativos).
2. Asegúrese de que el demonio de Docker (`dockerd`) cuente con permisos de ejecución y esté habilitado para iniciar con el sistema.
3. Integre el usuario local dentro del grupo de seguridad `docker` para permitir la interacción sin necesidad de elevar privilegios (`sudo`).

:::

## Preparación del orquestador {#sec-preparacion-docker}

Previo a la inicialización de las imágenes del curso, es indispensable dimensionar los recursos de almacenamiento y memoria del equipo local. Las imágenes políglotas y los motores espaciales del entorno consolidan un volumen de datos considerable:

* Se requiere un mínimo de **50 GB de espacio libre** en la unidad asignada a los discos virtuales.
* El despliegue integral de las dos imágenes base del curso genera un consumo acumulado de aproximadamente **45 GB**.

### Reasignación del almacenamiento de imágenes virtuales

Si la unidad de almacenamiento principal carece del espacio libre requerido, se debe reubicar el catálogo donde el orquestador almacena los discos virtuales (archivos con extensión `.vhdx`).

::: {.panel-tabset}

#### Sistema anfitrión (Windows)

Para mover la ubicación física del disco virtual de Docker Desktop a una unidad secundaria:

1. Abra la interfaz de **Docker Desktop**.
2. Haga clic en el ícono de engranaje en la barra superior para acceder a **Settings**.
3. En el panel lateral izquierdo, diríjase a **Resources** y seleccione la sección **Advanced**.
4. Localice el parámetro **Disk image location**.
5. Haga clic en el botón `Browse` y seleccione una carpeta en una unidad con suficiente capacidad de almacenamiento.
6. Haga clic en **Apply & restart** para que el servicio migre los datos y reinicie el motor.

#### Procedimiento general (agnóstico)

En configuraciones donde no se emplea Docker Desktop o se interactúa directamente con el demonio de Docker en el host:

1. Detenga el servicio `docker`.
2. Modifique el archivo `/etc/docker/daemon.json` agregando la directiva con la nueva ruta:

```json
#| label: setup-docker-daemon-dir
#| eval: false
{
  "data-root": "/<ruta_al_disco_secundario>/docker-data"
}

```

3. Mueva los archivos existentes e inicialice nuevamente el servicio.

:::

### Optimización de la memoria de intercambio (swap)

Para procesar conjuntos de datos ráster y vectoriales masivos sin interrupciones en R, Python o Julia, es imperativo configurar una partición de intercambio de alta capacidad. Esto evita la terminación forzada de contenedores por saturación de memoria RAM (*Out of Memory Killer*). Puede consultar más detalles técnicos en la @sec-limpieza-entorno.

::: {.panel-tabset}

#### Sistema anfitrión (Windows)

El ajuste del espacio de intercambio en Windows requiere configurar la memoria virtual del sistema operativo y los parámetros del subsistema WSL 2.

**Paso 1: Ajustar la memoria virtual en el sistema anfitrión**

1. En la barra de búsqueda de Windows, escriba y seleccione **"Ver la configuración avanzada del sistema"**.
2. En la pestaña **Opciones avanzadas**, ubique la sección **Rendimiento** y haga clic en el botón `Configuración`.
3. Diríjase a la pestaña **Opciones avanzadas**, vaya al bloque **Memoria virtual** y presione el botón `Cambiar`.
4. Desmarque la casilla "Administrar automáticamente el tamaño del archivo de paginación para todas las unidades".
5. Seleccione la unidad de almacenamiento principal o más veloz (SSD/NVMe), elija la opción **Tamaño personalizado** y defina los siguientes valores sugeridos (sujetos a la capacidad real del disco):
* **Tamaño inicial**: `16384` MB (16 GB).
* **Tamaño máximo**: `32768` MB (32 GB).


6. Presione el botón `Establecer`, luego `Aceptar` y reinicie el equipo.

**Paso 2: Configurar los recursos globales de WSL 2 (.wslconfig)**

Docker Desktop delega la gestión de RAM y swap en el archivo de configuración global del hipervisor:

1. Abra el explorador de archivos, ingrese `%USERPROFILE%` en la barra de direcciones y presione `Enter` para situarse en la raíz del usuario local.
2. Cree un archivo de texto con el nombre exacto `.wslconfig` (verifique que no conserve la extensión oculta `.txt`).
3. Incorpore la siguiente configuración para forzar el límite de RAM y dirigir el archivo de intercambio hacia un disco de alta velocidad:

```ini
#| label: setup-wslconfig
#| eval: false
[wsl2]
memory=12GB # RAM física asignada al subsistema de virtualización
swap=32GB   # Memoria de intercambio para contingencia analítica
swapfile=<unidad>:\\<ruta_al_directorio_con_espacio>\\swap.vhdx

```

4. Reinicie el subsistema desde la terminal de comandos ejecutando `wsl --shutdown`.

#### Procedimiento general (agnóstico)

En entornos basados directamente en Linux, el espacio de intercambio se dimensiona en el host mediante la creación de un archivo de paginación estructurado:

```bash
#| label: setup-create-swapfile
#| eval: false
# 1. Crear un archivo reservado de 32 GB
sudo fallocate -l 32G /swapfile

# 2. Asignar permisos estrictos de lectura y escritura
sudo chmod 600 /swapfile

# 3. Formatear y activar el espacio de intercambio
sudo mkswap /swapfile
sudo swapon /swapfile

```

:::

::: {.callout-tip}

#### Auditoría de recursos asignados

Para comprobar si la memoria RAM y el espacio de intercambio se encuentran disponibles dentro del contenedor o en la terminal integrada de Visual Studio Code, ejecute:

```bash
#| label: audit-memory-resources
#| eval: false
free -h

```

:::



## Descarga y preparación de archivos {#sec-descarga-tar-yml}

Ejecute los siguientes pasos para consolidar el entorno base:

1. **Obtención**: Descargue el archivo de recursos 

* Archivo [j_eval_j_plot.r](https://drive.google.com/file/d/1syuuciiso0c-F8TOQGRvi0A0tUv4v0RD/view?usp=sharing)
* Archivo [sig_unal_v1.12_completo_final.zip](https://drive.google.com/file/d/1PYeoMiDemTYgfc16i0A14pmNbydND2Oo/view?usp=sharing)
    * sig_unal_v1.12_completo_final.tar
    * docker-compose.yml
    * init_unal_spatialdb.sql
    * Dockerfile.postgis
* Archivo [backups_pg18.zip](https://drive.google.com/file/d/1uSrMPfbJKRQAphjzZxF_Fx7l6XBVGfZO/view?usp=sharing)

desde el siguiente link en Google Drive.
2. **Descompresión**: Extraiga el contenido del archivo comprimido `sig_unal_v1.12_completo_final.zip`.
3. **Estructura del directorio**:

   * `sig_unal_v1.12_completo_final.tar`: Imagen consolidada del servidor espacial y del entorno analítico.
   * `docker-compose.yml`: Archivo de orquestación de puertos, redes y volúmenes persistentes.
   * `init_unal_spatialdb.sql`: Esquema base de datos espacial en texto plano.
   * `j_eval_j_plot.r`: Funciones de intercomunicación entre R y Julia.

Asegure que el directorio de trabajo local cumpla las siguientes especificaciones:

- Exclusión estricta de directorios sincronizados en la nube.
- Rutas absolutas sin espacios en blanco, tildes ni caracteres especiales.
- Omisión de carpetas temporales del sistema anfitrión.

## Carga de contenedores orquestados {#sec-cargar-docker-tar}

Este procedimiento ejecuta el aprovisionamiento local de dos imágenes estructuradas y configura la red interna para garantizar la comunicación bidireccional. Desde la terminal integrada ubicada en la raíz del proyecto, ejecute:

```bash
#| label: docker-carga-imagenes
#| eval: false
docker load -i sig_unal_v1.12_completo_final.tar

```

> **Nota**: El intérprete de comandos pausará la respuesta hasta finalizar la descompresión binaria. Este proceso demanda tiempo y no debe interrumpirse bajo ninguna circunstancia.

## Contenedores instalados {#sec-contenedores-instalados}

Finalizada la inyección de imágenes, ejecute el siguiente comando para auditar la disponibilidad de recursos:

```bash
#| label: docker-verificacion-ps
#| eval: false
docker ps -a

```

1. Contenedor `contenedor_sig_unal` basado en la imagen `image_sig_unal:v1.11_produccion_final`, equipado con:

    * **Python**, **R** y **Julia** y librerías geoespaciales.
    * **Quarto** (Ver @sec-anexo-quarto).


2. Contenedor `contenedor_postgis_unal` basado en la imagen `postgis_unal:v1.12`, equipado con:

    * **PostgreSQL** y **PostGIS**, integrando el catálogo de estudio `sig_db_unal`.



## Puesta en marcha y detención de contenedores


### Inicio y reanudación de servicios {#sec-arrancar-contenedores}

Para la provisión inicial del entorno, creación de redes virtuales y construcción de los contenedores a partir de la configuración declarativa:

```bash
#| label: docker-up-servicios
#| eval: false
docker compose up -d

```

Este comando valida la configuración, crea los contenedores definidos si no existen y levanta los servicios en segundo plano.

Si los contenedores ya fueron creados previamente y únicamente se encuentran en estado suspendido, se reactivan sus procesos sin recrear las redes ni alterar su capa mutable mediante:

```bash
#| label: docker-start-servicios
#| eval: false
docker compose start

```

Una vez reactivada la instancia, abra Visual Studio Code en el sistema anfitrión, presione `Ctrl + Shift + P`, seleccione **Dev Containers: Attach to Running Container...** y elija `/contenedor_sig_unal` para retomar el espacio de trabajo con todas sus extensiones previas intactas.

### Detención y desmontaje de servicios {#sec-detener-contenedores}

Al finalizar la sesión ordinaria, se debe suspender la ejecución de los servicios enviando una señal de apagado controlado (`SIGTERM`):

```bash
#| label: docker-stop-servicios
#| eval: false
docker compose stop

```

Esta operación detiene los procesos del motor relacional preservando intacta la capa de lectura y escritura del contenedor, lo cual asegura que las extensiones instaladas y la configuración del entorno **persistan** para la siguiente sesión.

Por el contrario, si se requiere reiniciar la infraestructura a su estado base o liberar los recursos de red del sistema anfitrión, se procede con el desmontaje:

```bash
#| label: docker-down-servicios
#| eval: false
docker compose down

```

Esta instrucción detiene los servicios y destruye los contenedores y las redes virtuales asociadas. Aunque los datos espaciales almacenados en los volúmenes físicos de PostgreSQL permanecen intactos, la capa de escritura del contenedor **se elimina**, lo que requerirá reinstalar extensiones adicionales de Visual Studio Code al momento de volver a ejecutar `docker compose up -d`.

## Localización de archivos y persistencia

El directorio de trabajo local queda montado bidireccionalmente en la ruta `/home/rstudio/work` dentro de la imagen.

::: {.callout-important}

### Persistencia estructural

La topología garantiza que la caída del sistema anfitrión no comprometa las tablas almacenadas en el catálogo PostGIS ni los scripts depositados en `/home/rstudio/work`. El volumen configurado actúa como punto de montaje asíncrono.
:::

## Vinculación de contenedores en la terminal integrada {#sec-cargar-contenedores-vscode}

Para consolidar el entorno de desarrollo, acople el editor de texto (VSCode) directamente al núcleo del contenedor:

1. Levante los servicios mediante `docker compose up -d`.
2. Habilite la extensión *Dev Containers* en el equipo local.
3. Desde la paleta de comandos `Ctrl + Shift + P`, ejecute **Dev Containers: Attach to Running Container...** seleccionando el objetivo `contenedor_sig_unal`.
4. Verifique el directorio de montaje `/home/rstudio/work`.
5. Verifique las extensiones descritas en la @sec-extensiones-vscode, o directamente en la @tbl-extensiones.

## Configuración de puertos y topología de conectividad {#sec-puertos-conectividad}

El diseño de red de la infraestructura basada en contenedores implementa un esquema de reenvío de puertos (*port forwarding*) estructurado para mitigar colisiones con servicios preexistentes en el equipo local y garantizar el acceso simultáneo a los entornos analíticos y al motor relacional espacial.

| Servicio | Puerto anfitrión | Puerto contenedor | Propósito técnico | Dirección de enlace |
| :-- | :-: | :-: | :--- | :---- |
| **Jupyter Lab** | `8889` | `8888` | Computación interactiva y notebooks | `http://127.0.0.1:8889` |
| **Visor R (httpgd)** | `8788` | `8787` | Renderizado gráfico SVG interactivo | `http://127.0.0.1:8788` |
| **PostGIS (DB)** | `5434` | `5432` | Motor de base de datos espacial relacional | `127.0.0.1:5434` |

: Mapeo de puertos para los servicios de la infraestructura dockerizada {#tbl-puertos-red}

### Coexistencia de clústeres y resolución de red según el contexto de ejecución

La resolución de nombres de red y los puertos de conexión varían sustancialmente según la ubicación del proceso emisor:

* **Peticiones originadas desde el sistema anfitrión (*host*):**
  * **Motor PostgreSQL físico local:** Se canaliza a través de la dirección de bucle local `localhost` (`127.0.0.1`) en el puerto TCP estándar **`5432`**.
  * **Motor espacial en el contenedor (`db-postgis`):** Se intercepta mediante la interfaz expuesta por Docker en `127.0.0.1` sobre el puerto **`5434`** (`5434:5432`), evitando interferencias con el servicio local predeterminado.

* **Peticiones originadas desde la terminal integrada en el contenedor:**
  * **Motor PostgreSQL físico local:** Al encontrarse aislado de la interfaz de red del anfitrión, la dirección `127.0.0.1` apunta al propio contenedor. La conexión hacia el motor físico del host requiere resolver el alias DNS especial **`host.docker.internal`** en el puerto nativo **`5432`**.
  * **Motor espacial en el contenedor (`db-postgis`):** La comunicación entre contenedores adyacentes vinculados a la misma red virtual (*bridge*) se efectúa mediante el nombre del servicio **`db-postgis`** en su puerto nativo **`5432`** (el puerto `5434` no existe en el espacio de nombres interno de Docker).

| Vector de conexión | Destino del servicio | Host / Interfaz | Puerto | Función técnica |
| :--- | :--- | :--- | :---: | :--- |
| **Desde el sistema anfitrión** | Instancia física local | `localhost` | `5432` | Puerto estándar del servicio local |
| **Desde el sistema anfitrión** | Clúster en contenedor | `127.0.0.1` | `5434` | Puerto externo expuesto por Docker |
| **Desde la terminal del contenedor** | Instancia física local | `host.docker.internal` | `5432` | Puente hacia el PostgreSQL del anfitrión |
| **Desde la terminal del contenedor** | Clúster en contenedor | `db-postgis` | `5432` | Resolución DNS interna del clúster |

: Matriz de enrutamiento y puertos según el vector de ejecución {#tbl-enrutamiento-puertos}

## Credenciales de la base de datos {#sec-credenciales}

Parámetros de conexión estándar para utilidades cliente (QGIS, utilidades CLI, Python, R, Julia):

* **Catálogo:** `sig_db_unal`
* **Superusuario:** `profe_unal`
* **Autenticación:** `geomatica2025`
* **Enlace externo:** `127.0.0.1` (Puerto `5434`)
* **Enlace interno:** `db-postgis` (Puerto `5432`)

## Lógica de arquitectura (Dockerfile y docker-compose) {#sec-arquitectura-puertos}

La configuración se encuentra segmentada para garantizar aislamiento de memoria:

1. **Dockerfile**: Implementa directivas `EXPOSE 8888` y `EXPOSE 8787`, declarando canales de escucha independientes para evitar colisiones entre el servidor HTTP de R y el núcleo Jupyter.
2. **Orquestador YAML**: Ejecuta el mapeo `5434:5432` previniendo conflictos en el equipo local si existe un demonio PostgreSQL ejecutándose en el canal `5432`.

## Guía de acceso: topología de red {#sec-guia-acceso}

La comunicación difiere dependiendo del vector de entrada:

### Conexión desde el sistema anfitrión (Exterior)

Las conexiones emitidas desde clientes locales deben apuntar a los puertos de exposición definidos en el enrutador virtual: Jupyter (`8889`), Base de Datos (`5434`).

### Conexión intraclúster (Interior)
 
Los algoritmos ejecutados desde los cuadernos de trabajo operan bajo la red puente nativa. Por ende, deben apuntar directamente al host `db-postgis` en el puerto `5432`.

## Limpieza del entorno de contenedores {#sec-limpieza-entorno}

El ciclo de vida del desarrollo geoespacial genera capas de construcción, volúmenes de datos transitorios e imágenes intermedias que pueden comprometer el almacenamiento del equipo local o inducir conflictos durante la auto-inicialización declarativa de bases de datos.

A continuación se formaliza el protocolo sistemático para el diagnóstico, desmantelamiento ordenado, purga exhaustiva y auditoría de almacenamiento del subsistema de contenedores desde la terminal integrada de Visual Studio Code.

### Diagnóstico de consumo de almacenamiento

Previo a cualquier operación de limpieza, se debe inspeccionar la volumetría física asignada a contenedores, imágenes, volúmenes locales y caché de compilación (*build cache*):

```{bash}
#| label: docker-diagnostico-almacenamiento
#| eval: false
#| echo: true
# Resumen global de espacio utilizado por subsistema
docker system df

# Desglose detallado por objeto, identificador y estado de referencia
docker system df -v

```

### Desmantelamiento de servicios y volúmenes vinculados

Cuando un proyecto opera mediante Docker Compose, el desmantelamiento debe garantizar la remoción coordinada de contenedores, redes virtuales en puente y volúmenes de datos persistentes (`-v`):

```{bash}
#| label: docker-compose-desmantelamiento
#| eval: false
#| echo: true
# Detención y remoción de servicios, redes del proyecto y volúmenes nombrados
docker compose down -v --remove-orphans

```

> **Nota técnica:** La bandera `-v` asegura la eliminación de volúmenes persistentes (tales como `postgis_data_unal`), permitiendo que despliegues posteriores ejecuten scripts de inicialización DDL/DML desde un estado completamente limpio.

### Purga integral de artefactos y caché de compilación

> **Advertencia:** Las siguientes instrucciones eliminan de forma irreversible contenedores detenidos, redes virtuales huérfanas, volúmenes sin asociar, imágenes sin etiquetar y cachés de construcción.

Para liberar el almacenamiento físico y purgar el subsistema BuildKit:

```{bash}
#| label: docker-purga-sistema-buildkit
#| eval: false
#| echo: true
# 1. Purga exhaustiva de contenedores inactivos, redes, volúmenes huérfanos e imágenes no referenciadas
docker system prune -a --volumes -f

# 2. Eliminación de la memoria caché de construcción de BuildKit
docker builder prune -a -f

```

En escenarios administrativos donde se requiera forzar la remoción absoluta de la totalidad de objetos presentes en el subsistema (incluyendo imágenes base activas), se pueden encadenar las utilidades de listado y remoción forzada:

```{bash}
#| label: docker-purga-destructiva-total
#| eval: false
#| echo: true
# Detención forzada y eliminación de todos los contenedores registrados
docker stop $(docker ps -a -q) 2>/dev/null || true
docker rm -f $(docker ps -a -q) 2>/dev/null || true

# Eliminación de la totalidad de imágenes locales y volúmenes huérfanos
docker rmi -f $(docker images -a -q) 2>/dev/null || true
docker volume rm $(docker volume ls -q) 2>/dev/null || true

```

### Auditoría y confirmación de estado limpio

Finalizado el procedimiento de saneamiento, se valida que el subsistema de contenedores reporte una ocupación volumétrica nula (`0 B`) y que no existan entidades residuales en memoria o disco:

```{bash}
#| label: docker-audit-confirmacion-limpio
#| eval: false
#| echo: true
# Verificación de volumetría del motor
docker system df

# Comprobación de inventarios vacíos por subsistema
docker ps -a
docker images -a
docker volume ls

```


## Verificación de registros de instalación

Para inspeccionar el flujo estándar de errores y salidas emitidas por el contenedor principal:

```bash
#| label: docker-logs-sig
#| eval: false
docker logs contenedor_sig_unal

```

## Comandos de orquestación

Matriz referencial de utilidades CLI para la gestión de contenedores.

| Fase | Invocación CLI | Función técnica |
| -- | --- | ---- |
| **Construcción** | `docker compose up --build -d` | Compilación integral y mapeo del árbol de imágenes. |
| **Inicio** | `docker compose up -d` | Reactivación silenciosa en segundo plano. |
| **Monitoreo** | `docker logs -f contenedor_sig_unal` | Captura continua del flujo estándar de salida. |
| **Auditoría R** | `docker exec -i -t contenedor_sig_unal R -e "library(sf)"` | Validación de motores PROJ/GEOS acoplados al R-Engine. |

: Matriz de comandos de orquestación de Docker {#tbl-comandos-docker}

## Resumen de la infraestructura instalada

| Componente | Especificación | Dependencia técnica |
| - | -- | ---- |
| **R Engine** | `4.3.3` (Angel Food Cake) | `JuliaConnectoR` acoplado al servidor `httpgd`. |
| **Julia Stack** | `v1.10.x` | `ArchGDAL 3.12.1` resuelto vía enlaces simbólicos. |
| **Python Stack** | `3.12.x` | GeoPandas, Shapely y conectores `psycopg2`. |
| **Persistencia** | Volumen gestionado | Montaje bidireccional estable sobre `/home/rstudio/work`. |

: Inventario de arquitectura y componentes del laboratorio {#tbl-inventario-software}

## Validación del entorno Python

### Ingreso al entorno interactivo {#sec-acceder-jupyterlab}

Desde la terminal integrada, asegure el arranque del servidor forzando los tokens de seguridad y los parámetros de red (evitando concatenaciones en las banderas cortas):

```bash
#| label: setup-jupyter-arranque
#| eval: false
jupyter lab --ip 0.0.0.0 --port 8889 --no-browser --allow-root --ServerApp.token geomatica2025

```

Acceso web: `http://127.0.0.1:8889` (Token: `geomatica2025`).

### Guía visual del entorno

La carpeta `/home/rstudio/work/` opera como un espejo asíncrono hacia el almacenamiento local, garantizando inmutabilidad frente a cortes abruptos del contenedor.

## Validación del entorno R

### Visualización interactiva con httpgd {#sec-httpgd-config}

Al tratarse de una infraestructura sin servidor X11 nativo (*headless*), se implementa `httpgd` como puente de renderizado de gráficos vectoriales para VSCode.

#### Inicio del servidor gráfico

Active el motor explícitamente al inicio de cada sesión interactiva:

```r
#| label: setup-httpgd-arranque
#| eval: false
httpgd::hgd()
httpgd::hgd_details()

```

#### Resolución de conflictos

Si el sistema de red omite la inyección del puerto `8787`, aplique una directiva forzada inhabilitando transitoriamente los tokens de seguridad de capa:

```r
#| label: setup-httpgd-fuerza
#| eval: false
httpgd::hgd(host = "0.0.0.0", port = 8787, token = FALSE)

```

#### Función crítica del servidor gráfico

El dispositivo `httpgd` convierte el buffer en elementos interactivos de estándar SVG renderizables directamente por la API visual del editor, esquivando las restricciones naturales del despliegue en contenedores aislados.

## Validación del entorno Julia

### Ejecución de rutinas mediante evaluación cruzada

El sistema procesa bloques Quarto mediante `knitr`. La incompatibilidad histórica de `JuliaCall` con punteros de librerías geoespaciales requirió la creación de los wrappers `j_eval` y `j_plot` soportados por `JuliaConnectoR`.

Invoque las directivas de comunicación interlenguaje mediante el archivo base:

```r
#| label: setup-julia-puente
#| code-fold: true
#| results: asis
source("../j_eval_j_plot.r")

```

## Verificación de conectividad multilenguaje

::: panel-tabset

### Python

::: {.content-visible when-format="html"}
::: {.callout-tip collapse="true" icon="false"}

#### Código crudo de auditoría (Python)

```{python}
#| label: setup-python-auditoria-codigo
#| eval: false
import psycopg2
import geopandas as gpd
from shapely.geometry import Point

try:
    conn = psycopg2.connect(
        host="db-postgis", 
        dbname="sig_db_unal", 
        user="profe_unal", 
        password="geomatica2025"
    )
    print("Conexión a base de datos espacial exitosa")
    conn.close()
except Exception as e:
    print(f"Error relacional: {e}")

```

:::
:::

```{python}
#| label: audit-python-auditoria-ejecucion
#| eval: true
#| echo: true
import psycopg2
import geopandas as gpd
from shapely.geometry import Point

try:
    conn = psycopg2.connect(
        host="db-postgis", 
        dbname="sig_db_unal", 
        user="profe_unal", 
        password="geomatica2025"
    )
    print("Conexión a base de datos espacial exitosa")
    conn.close()
except Exception as e:
    print(f"Error relacional: {e}")

```

### R

::: {.content-visible when-format="html"}
::: {.callout-tip collapse="true" icon="false"}

#### Código crudo de auditoría (R)

```{r}
#| label: setup-r-auditoria-codigo
#| eval: false
#| echo: true
library(sf)
library(terra)

conf <- sf_extSoftVersion()
cat(paste0("Motores de sistema detectados:\n",
           " - GDAL: ", conf["GDAL"], "\n",
           " - GEOS: ", conf["GEOS"], "\n",
           " - PROJ: ", conf["PROJ"], "\n"))

```

:::
:::

```{r}
#| label: audit-r-auditoria-ejecucion
#| eval: true
#| echo: true
library(sf)
library(terra)

conf <- sf_extSoftVersion()
cat(paste0("Motores de sistema detectados:\n",
           " - GDAL: ", conf["GDAL"], "\n",
           " - GEOS: ", conf["GEOS"], "\n",
           " - PROJ: ", conf["PROJ"], "\n"))

```


### Julia

::: {.content-visible when-format="html"}
::: {.callout-tip collapse="true" icon="false"}

#### Código crudo de auditoría (Julia)

```{julia}
#| label: audit-julia-auditoria-codigo
#| fig-align: center
#| out-width: "85%"
#| eval: false

using LibPQ
using DataFrames
using Plots

ENV["GKSwstype"] = "100"
gr()

plots_dir = get(ENV, "PLOTS_DIR", ".")
mkpath(plots_dir)

try
    canal = LibPQ.Connection("host=db-postgis port=5432 dbname=sig_db_unal user=profe_unal password=geomatica2025")
    
    query_autoridades = """
        SELECT auth_name, count(*)::int AS total
        FROM spatial_ref_sys
        WHERE auth_name IS NOT NULL
        GROUP BY auth_name
        ORDER BY total DESC
        LIMIT 5;
    """
    df_autoridades = DataFrame(execute(canal, query_autoridades))
    
    query_longitudes = """
        SELECT length(coalesce(srtext, ''))::int AS longitud_wkt
        FROM spatial_ref_sys
        WHERE auth_name = 'EPSG' AND srtext IS NOT NULL
        LIMIT 1000;
    """
    df_longitudes = DataFrame(execute(canal, query_longitudes))
    
    close(canal)
    
    p1 = bar(
        df_autoridades.auth_name, 
        df_autoridades.total, 
        color = :steelblue,
        xlabel = "Autoridad", 
        ylabel = "Cantidad de SRID", 
        title = "Sistemas de referencia por autoridad",
        xrotation = 15,
        legend = false
    )
    
    p2 = histogram(
        df_longitudes.longitud_wkt, 
        bins = 20, 
        color = :coral,
        xlabel = "Longitud WKT EPSG (campo srtext)", 
        ylabel = "Frecuencia", 
        title = "Distribución de longitud WKT",
        legend = false
    )
    
    # Composición con márgenes explícitos en milímetros
    p_final = plot(
        p1, p2, 
        layout = (1, 2), 
        size = (900, 420),
        left_margin = 12Plots.mm,
        bottom_margin = 10Plots.mm,
        top_margin = 6Plots.mm,
        right_margin = 6Plots.mm
    )
    savefig(p_final, joinpath(plots_dir, "temp_grafica_julia.png"))
    
catch fallo_conexion
    println("Fallo de interconexión o consulta al orquestador: ", fallo_conexion)
end

```

:::
:::

```{r}
#| label: audit-julia-auditoria-ejecucion
#| fig-align: center
#| out-width: "80%"
#| results: asis
#| code-fold: true

j_eval('
using LibPQ
using DataFrames

try
    canal = LibPQ.Connection("host=db-postgis port=5432 dbname=sig_db_unal user=profe_unal password=geomatica2025")
    resultado_sql = execute(canal, "SELECT PostGIS_Version();")
    dataframe_version = DataFrame(resultado_sql)
    println("Enlace de datos espacial establecido con éxito en Julia. Versión: ", dataframe_version[1, 1])
    close(canal)
catch fallo_conexion
    println("Fallo de interconexión al orquestador: ", fallo_conexion)
end
')

```

<!-- ```{r}
#| label: graficos_julia_ejecucion_j_eval
#| echo: true
#| results: asis
#| code-fold: true
#| fig-align: "center"
#| out-width: "90%"

# 1. Extracción de estadísticas en Julia, generación gráfica y exportación a disco
j_eval('
ENV["GKSwstype"] = "100" 
using LibPQ
using DataFrames
using Plots
gr()

plots_dir = get(ENV, "PLOTS_DIR", ".")
mkpath(plots_dir)

try
    canal = LibPQ.Connection("host=db-postgis port=5432 dbname=sig_db_unal user=profe_unal password=geomatica2025")
    
    # Extracción de frecuencias por autoridad
    query_autoridades = "SELECT auth_name, count(*)::int AS total FROM spatial_ref_sys WHERE auth_name IS NOT NULL GROUP BY auth_name ORDER BY total DESC LIMIT 5;"
    df_autoridades = DataFrame(execute(canal, query_autoridades))
    
    # Extracción de distribución de longitudes textuales
    query_longitudes = "SELECT length(coalesce(srtext, \'\'))::int AS longitud_wkt FROM spatial_ref_sys WHERE auth_name = \'EPSG\' AND srtext IS NOT NULL LIMIT 1000;"
    df_longitudes = DataFrame(execute(canal, query_longitudes))
    
    close(canal)
    
    local p1 = bar(
        df_autoridades.auth_name, 
        df_autoridades.total, 
        color = :steelblue,
        xlabel = "Autoridad", 
        ylabel = "Cantidad de SRID", 
        title = "Sistemas de referencia por autoridad",
        xrotation = 15,
        legend = false
    )
    
    local p2 = histogram(
        df_longitudes.longitud_wkt, 
        bins = 20, 
        color = :coral,
        xlabel = "Longitud WKT EPSG (campo srtext)", 
        ylabel = "Frecuencia", 
        title = "Distribución de longitud WKT",
        legend = false
    )
    
    # Inclusión de márgenes para prevenir clipping en el backend GR
    local p_final = plot(
        p1, p2, 
        layout = (1, 2), 
        size = (900, 420),
        left_margin = 12Plots.mm,
        bottom_margin = 10Plots.mm,
        top_margin = 6Plots.mm,
        right_margin = 6Plots.mm
    )
    savefig(p_final, joinpath(plots_dir, "temp_grafica_julia.png"))
    
catch fallo_proceso
    println("Error durante la extracción o graficación en Julia: ", fallo_proceso)
end
')

# 2. Renderizado dinámico de la figura generada en el entorno Knitr
plots_dir <- Sys.getenv("PLOTS_DIR", unset = ".")
ruta_img <- file.path(plots_dir, "temp_grafica_julia.png")
knitr::include_graphics(ruta_img)

``` -->

<!-- ```{r}
#| label: graficos_julia_ejecucion_j_plot
#| echo: true
#| results: asis
#| code-fold: true
#| fig-align: "center"
#| out-width: "90%"

# 1. Definición de rutas y creación de directorios desde el entorno R
plots_dir <- Sys.getenv("PLOTS_DIR", unset = ".")
if (!dir.exists(plots_dir)) dir.create(plots_dir, recursive = TRUE)
ruta_img <- file.path(plots_dir, "temp_grafica_julia.png")

# 2. Extracción de estadísticas en Julia, generación gráfica y renderizado
# Advertencia: Todo el código de Julia, incluyendo plot(), debe permanecer dentro de las comillas simples
j_plot('
ENV["GKSwstype"] = "100" 
using LibPQ
using DataFrames
using Plots
gr()

try
    canal = LibPQ.Connection("host=db-postgis port=5432 dbname=sig_db_unal user=profe_unal password=geomatica2025")
    
    # Extracción de frecuencias por autoridad
    query_autoridades = "SELECT auth_name, count(*)::int AS total FROM spatial_ref_sys WHERE auth_name IS NOT NULL GROUP BY auth_name ORDER BY total DESC LIMIT 5;"
    df_autoridades = DataFrame(execute(canal, query_autoridades))
    
    # Uso estricto de una sola barra invertida para escapar comillas simples en la sintaxis de R
    query_longitudes = "SELECT length(coalesce(srtext, \\\'\\\'))::int AS longitud_wkt FROM spatial_ref_sys WHERE auth_name = \\\'EPSG\\\' AND srtext IS NOT NULL LIMIT 1000;"
    df_longitudes = DataFrame(execute(canal, query_longitudes))
    
    close(canal)
    
    local p1 = bar(
        df_autoridades.auth_name, 
        df_autoridades.total, 
        color = :steelblue,
        xlabel = "Autoridad", 
        ylabel = "Cantidad de SRID", 
        title = "Sistemas de referencia por autoridad",
        xrotation = 15,
        legend = false
    )
    
    local p2 = histogram(
        df_longitudes.longitud_wkt, 
        bins = 20, 
        color = :coral,
        xlabel = "Longitud WKT EPSG (campo srtext)", 
        ylabel = "Frecuencia", 
        title = "Distribución de longitud WKT",
        legend = false
    )
    
    plot(
        p1, p2, 
        layout = (1, 2), 
        size = (900, 420),
        left_margin = 12Plots.mm,
        bottom_margin = 10Plots.mm,
        top_margin = 6Plots.mm,
        right_margin = 6Plots.mm
    )
    
catch fallo_proceso
    println("Error durante la extracción o graficación en Julia: ", fallo_proceso)
end
', n = ruta_img, w = 900, h = 420)
```
 -->

```{r}
#| label: graficos_julia_ejecucion_j_plot
#| echo: true
#| results: asis
#| code-fold: true
#| fig-align: "center"
#| out-width: "90%"

plots_dir <- Sys.getenv("PLOTS_DIR", unset = ".")
if (!dir.exists(plots_dir)) dir.create(plots_dir, recursive = TRUE)
ruta_img <- file.path(plots_dir, "temp_grafica_julia.png")

j_plot('
ENV["GKSwstype"] = "100" 
using LibPQ
using DataFrames
using Plots
gr()

try
    canal = LibPQ.Connection("host=db-postgis port=5432 dbname=sig_db_unal user=profe_unal password=geomatica2025")
    
    query_autoridades = "SELECT auth_name, count(*)::int AS total FROM spatial_ref_sys WHERE auth_name IS NOT NULL GROUP BY auth_name ORDER BY total DESC LIMIT 5;"
    df_autoridades = DataFrame(execute(canal, query_autoridades))
    
    query_longitudes = "SELECT length(coalesce(srtext, \\\'\\\'))::int AS longitud_wkt FROM spatial_ref_sys WHERE auth_name = \\\'EPSG\\\' AND srtext IS NOT NULL LIMIT 1000;"
    df_longitudes = DataFrame(execute(canal, query_longitudes))
    
    close(canal)
    
    local p1 = bar(
        df_autoridades.auth_name, 
        df_autoridades.total, 
        color = :steelblue,
        xlabel = "Autoridad", 
        ylabel = "Cantidad de SRID", 
        title = "Sistemas de referencia por autoridad",
        xrotation = 15,
        legend = false
    )
    
    local p2 = histogram(
        df_longitudes.longitud_wkt, 
        bins = 20, 
        color = :coral,
        xlabel = "Longitud WKT EPSG (campo srtext)", 
        ylabel = "Frecuencia", 
        title = "Distribución de longitud WKT",
        legend = false
    )
    
    plot(
        p1, p2, 
        layout = (1, 2), 
        size = (900, 420),
        left_margin = 12Plots.mm,
        bottom_margin = 10Plots.mm,
        top_margin = 6Plots.mm,
        right_margin = 6Plots.mm
    )
    
catch fallo_proceso
    println("Error durante la extracción o graficación en Julia: ", fallo_proceso)
    
    # Gráfico de contingencia para evitar el colapso del compilador
    plot(
        title = "Fallo en ejecución de bloque espacial",
        framestyle = :none,
        annotations = (0.5, 0.5, text("Error: " * string(fallo_proceso), 10, :red, :center)),
        legend = false,
        size = (900, 420)
    )
end
', n = ruta_img, w = 900, h = 420)
```

:::